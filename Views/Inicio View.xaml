<UserControl
    x:Class="ZxStudio.Views.InicioView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="600"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">

        <StackPanel Margin="40">


            <Image
                Width="430"
                Height="100"
                Source="/Resources/feyalegria.png"
                Stretch="Uniform" />


            <Separator Margin="0,0,0,25" Background="#CCCCCC" />

            <StackPanel Margin="0,0,0,25" Orientation="Horizontal">
                <Button
                    Width="160"
                    Content="NUEVO PROYECTO"
                    Style="{StaticResource ModernButtonStyle}" />
                <Button
                    Width="160"
                    Content="ABRIR EXISTENTE"
                    Style="{StaticResource ModernButtonStyle}" />
            </StackPanel>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Border Style="{StaticResource CardBorder}">
                    <StackPanel Margin="20">
                        <TextBlock
                            FontSize="14"
                            FontWeight="Bold"
                            Foreground="#888"
                            Text="ESTADISTICAS" />
                        <TextBlock
                            FontSize="36"
                            FontWeight="Bold"
                            Foreground="{StaticResource PrimaryGreyBrush}"
                            Text="5" />
                        <TextBlock
                            FontSize="12"
                            Foreground="#666"
                            Text="Proyectos Activos" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardBorder}">
                    <StackPanel Margin="20">
                        <TextBlock
                            FontSize="14"
                            FontWeight="Bold"
                            Foreground="#888"
                            Text="PROXIMAS TAREAS" />
                        <TextBlock
                            Margin="0,10,0,0"
                            FontSize="20"
                            FontWeight="SemiBold"
                            Foreground="{StaticResource PrimaryGreyBrush}"
                            Text="Revisión Código" />
                        <TextBlock
                            Margin="0,5,0,0"
                            FontSize="12"
                            Foreground="#D9534F"
                            Text="Entrega: Mañana" />
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>