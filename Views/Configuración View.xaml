<UserControl x:Class="ZxStudio.Views.ConfiguracionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- Re-defining brushes and styles for stand-alone view context -->
        <SolidColorBrush x:Key="PrimaryGreyBrush" Color="#2D2D2D"/>
        <Style x:Key="ModernButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource PrimaryGreyBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </UserControl.Resources>

    <StackPanel Margin="40">
        <TextBlock Text="Configuración" FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryGreyBrush}" Margin="0,0,0,20"/>

        <Border Style="{StaticResource CardBorder}">
            <Grid Margin="15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Tema Oscuro" FontWeight="Bold" Foreground="{StaticResource PrimaryGreyBrush}"/>
                    <TextBlock Text="Cambiar apariencia visual" FontSize="12" Foreground="#888"/>
                </StackPanel>
                <CheckBox Grid.Column="1" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <Button Content="GUARDAR CAMBIOS" Style="{StaticResource ModernButtonStyle}" HorizontalAlignment="Left" Width="180" Margin="5,20,0,0"/>
    </StackPanel>
</UserControl>