<UserControl x:Class="ZxStudio.Views.BusquedaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- Re-defining styles/brushes for stand-alone view context -->
        <SolidColorBrush x:Key="PrimaryGreyBrush" Color="#2D2D2D"/>
        <Style x:Key="ModernButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource PrimaryGreyBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
        <Style x:Key="ModernTextBox" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <!-- Template simplified for brevity, relying on MainWindow for full definition -->
        </Style>
    </UserControl.Resources>

    <StackPanel Margin="40">
        <TextBlock Text="Búsqueda Avanzada" FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryGreyBrush}"/>

        <Grid Margin="0,20,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Style="{StaticResource ModernTextBox}" Text="Escribe aquí..."/>

            <Button Grid.Column="1" Content="BUSCAR" Style="{StaticResource ModernButtonStyle}" Margin="10,0,0,0"/>
        </Grid>

        <StackPanel Orientation="Horizontal">
            <Button Content="POR CARPETA" Style="{StaticResource ModernButtonStyle}"/>
            <Button Content="POR ETIQUETA" Style="{StaticResource ModernButtonStyle}"/>
        </StackPanel>
    </StackPanel>
</UserControl>