<UserControl x:Class="ZxStudio.Views.ImagenesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- Re-defining brushes and styles for stand-alone view context -->
        <SolidColorBrush x:Key="PrimaryGreyBrush" Color="#2D2D2D"/>
        <SolidColorBrush x:Key="SecondaryGreyBrush" Color="#404040"/>
        <SolidColorBrush x:Key="AccentGreyBrush" Color="#5A5A5A"/>
        <Style x:Key="ModernButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource PrimaryGreyBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
    </UserControl.Resources>

    <StackPanel Margin="40">
        <Grid Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Galería de Imágenes" FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryGreyBrush}"/>
            <Button Grid.Column="1" Content="AGREGAR" Style="{StaticResource ModernButtonStyle}"/>
        </Grid>

        <UniformGrid Columns="3" Rows="2">
            <Border Background="{StaticResource AccentGreyBrush}" CornerRadius="5" Margin="0,0,10,10" Height="120">
                <TextBlock Text="IMG 1" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
            <Border Background="{StaticResource SecondaryGreyBrush}" CornerRadius="5" Margin="0,0,10,10" Height="120">
                <TextBlock Text="IMG 2" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
            <Border Background="{StaticResource AccentGreyBrush}" CornerRadius="5" Margin="0,0,0,10" Height="120">
                <TextBlock Text="IMG 3" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
            <Border Background="{StaticResource SecondaryGreyBrush}" CornerRadius="5" Margin="0,0,10,0" Height="120">
                <TextBlock Text="IMG 4" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
            <Border Background="{StaticResource AccentGreyBrush}" CornerRadius="5" Margin="0,0,10,0" Height="120">
                <TextBlock Text="IMG 5" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
            <Border Background="{StaticResource SecondaryGreyBrush}" CornerRadius="5" Margin="0,0,0,0" Height="120">
                <TextBlock Text="IMG 6" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
        </UniformGrid>
    </StackPanel>
</UserControl>